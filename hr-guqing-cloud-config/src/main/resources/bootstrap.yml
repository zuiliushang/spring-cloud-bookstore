spring:
 config:
  name: guqing-config-center
 cloud:
  label: master
  config:
   server:
    git:
     uri: G://guqing-config-repo/# https://github.com/zuiliushang/guqing-config-repo
     # force-pull: true
     searchPaths: guqing-config